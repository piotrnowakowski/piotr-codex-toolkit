interface:
  display_name: "Browser Demo Recorder"
  short_description: "Record verified browser flows with timed findings"
  default_prompt: "Use $browser-demo-recorder to record this browser flow, verify the video, and return a structured report with timecoded findings."
